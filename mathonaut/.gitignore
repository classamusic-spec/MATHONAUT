# dependencies
node_modules/

# build artifact — the bundled core, regenerated by `npm run build:core` / `npm test`
testing/game.cjs

# web build: index.html is the committed, self-contained deliverable; these are
# regenerable intermediates
web/artifact.html
web/screenshot-run.png

# misc
*.log
.DS_Store
